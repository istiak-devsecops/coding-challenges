# script 1 pseudocode

'''
if there is not 3 arguments
    give user with the expected input
    stop the program

assaign the first arguments to file_path variable
assaign 2nd arguments or flags to flags variable

def count_lines function, take filepath as arguments
    with open the filepath and read it as file
        return sum of every line count

def count_words function and take filepath as arguments
    with open the filepath and read it as file
        return sum of every word 

if file_path is not a file
    print output file does not exist
    stop the program

if flag == "--count-lines":
    print(call line count function)
elif flag == "--count-words":
    print(call word count function)
else:
    print(ERROR: unknow flag)
    stop the program
'''

