# Shell Command Executor with Logging

Goal: Run a shell command passed via CLI and log the output.
Steps:
- Accept a shell command via sys.argv.
- Run it using subprocess.run() and capture output.
- Save stdout and stderr to a log file with a timestamp

