Directory Backup Script**

- **Args:** `--source`, `--dest`, `--compress` (flag)
    
- **Example:**  
    `python backup.py --source /etc --dest /tmp/backups --compress`
    
- **Goal:** Copy directories and optionally create a `.tar.gz`.